name: Build & Publish GemSuite Installer

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
# ------------------------------------------------------------
# 1️⃣ BUILD WINDOWS INSTALLER
# ------------------------------------------------------------
  build-windows:
    name: Build Windows Installer
    runs-on: windows-latest

    outputs:
      version: ${{ steps.meta.outputs.version }}

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build production installer
        run: node scripts/dist.js
        env:
          NODE_ENV: production

      - name: Read app version
        id: meta
        shell: bash
        run: |
          VERSION=$(node -p "require('./apps/desktop/package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: gemsuite-installer
          path: |
            apps/desktop/release/*.exe
            apps/desktop/release/latest.yml

# ------------------------------------------------------------
# 2️⃣ PUBLISH TO REPO B GITHUB PAGES
# ------------------------------------------------------------
  publish-pages:
    name: Publish Installer to Repo B GitHub Pages
    runs-on: ubuntu-latest
    needs: build-windows

    steps:
      - name: Checkout Repo B
        uses: actions/checkout@v4
        with:
          repository: akash-srivastava-pvt/Apps  # <-- Repo B
          token: ${{ secrets.RELEASE_PAT }}     # <-- PAT with write access to Repo B
          path: repo-b
          fetch-depth: 0

      - name: Download installer artifact
        uses: actions/download-artifact@v4
        with:
          name: gemsuite-installer
          path: site/GemSuite/${{ needs.build-windows.outputs.version }}

      - name: Configure git identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Copy installer to Repo B
        run: |
          mkdir -p repo-b/GemSuite/${{ needs.build-windows.outputs.version }}
          cp -r site/GemSuite/${{ needs.build-windows.outputs.version }}/* repo-b/GemSuite/${{ needs.build-windows.outputs.version }}/

      - name: Commit & push to gh-pages
        run: |
          cd repo-b
          git checkout -B gh-pages
          git add GemSuite
          git commit -m "Release v${{ needs.build-windows.outputs.version }}" || echo "No changes to commit"
          git push -u origin gh-pages --force

      - name: Output direct installer URL
        run: |
          echo "--------------------------------------------------"
          echo "Direct installer URL (NO redirect):"
          echo "https://akash-srivastava-pvt.github.io/Apps/GemSuite/${{ needs.build-windows.outputs.version }}/GemSuite%20Setup%20${{ needs.build-windows.outputs.version }}.exe"
          echo "--------------------------------------------------"
